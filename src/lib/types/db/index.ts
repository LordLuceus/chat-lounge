export enum AIProvider {
  Mistral = "mistral",
  OpenAI = "openai",
  ElevenLabs = "elevenlabs",
  Google = "google",
  Anthropic = "anthropic"
}

export enum Visibility {
  Public = "public",
  Private = "private",
  Hidden = "hidden"
}

export enum AgentType {
  Default = "default",
  Character = "character"
}
